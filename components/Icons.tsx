// import { LucideProps, UserPlus } from 'lucide-react'

// import { PersonIcon, PlusIcon, SunIcon } from "@radix-ui/react-icons";
import { LucideProps, PlusIcon, SunIcon, UserPlus } from "lucide-react";

export const Icons = {
    SunIcon,
    PlusIcon,
    UserPlus,
};

export type Icon = keyof typeof Icons;
