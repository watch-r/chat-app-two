import React from "react";

interface pageProps {
    testId: string;
}
const ChatDetailsPageTest = ({ testId }: pageProps) => {
    return <div>ChatDetailsPageTest</div>;
};

export default ChatDetailsPageTest;
